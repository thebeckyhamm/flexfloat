{
  "name": "flexfloat",
  "version": "0.0.0",
  "description": "grid system using flexbox that falls back to floats",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Becky Hamm",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-plumber": "^0.6.6",
    "gulp-sass": "^1.3.3"
  }
}
